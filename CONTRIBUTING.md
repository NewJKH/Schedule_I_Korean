# 기여 규칙

Schedule I 한글패치에 기여할 때 지키는 규칙입니다.

## 브랜치

`main` 에 직접 푸시하지 않습니다. 항상 브랜치를 따고 PR로 병합합니다.

| 접두사 | 용도 | 예 |
|---|---|---|
| `feat/` | 기능 추가 | `feat/melonloader-support` |
| `fix/` | 버그 수정 | `fix/tmp-harmony-hook` |
| `docs/` | 문서만 | `docs/contributing` |
| `refactor/` | 동작 변화 없는 정리 | `refactor/split-installer` |
| `chore/` | 빌드·설정·잡일 | `chore/bump-melonloader` |

이름은 kebab-case로 짧게 씁니다. 이슈가 있으면 브랜치 대신 PR 본문에서 연결합니다.

## 커밋

[Conventional Commits](https://www.conventionalcommits.org/) 를 따릅니다.

```
<타입>(<범위>): <제목>

<본문 — 왜 고쳤는지, 무엇을 바꿨는지>
```

- 타입: `feat`, `fix`, `docs`, `refactor`, `chore`, `test`
- 범위(선택): `plugin`, `installer`, `font`, `scripts`, `translation`
- 제목: **한국어로 씁니다.** 마침표 없이 72자 이내, 명령형("추가한다"보다 "추가")
- 본문: 무엇을 했는지보다 **왜 했는지**를 씁니다. 로그·에러 메시지를 인용하면 좋습니다
- 커밋 하나는 되돌릴 수 있는 단위 하나입니다. 관련 없는 변경을 섞지 않습니다

```
fix(plugin): MelonLoader에서 TMP 텍스트를 직접 후킹해 즉시 번역

MelonLoader의 Il2CppInterop은 비유니티 네임스페이스에 Il2Cpp 접두사를
붙이는데, XUnity는 "TMPro.TMP_Text" 로 타입을 찾기 때문에 후킹을 건너뛴다.
```

## PR

- PR 하나는 목적 하나입니다. 리팩터링과 기능 추가를 섞지 않습니다
- 본문에 **무엇을 / 왜 / 어떻게 / 검증** 을 씁니다
- 관련 이슈는 `Closes #N` (해결) 또는 `Refs #N` (관련)으로 연결합니다
- 설치기·플러그인을 건드렸으면 **실제 게임에 설치해 확인한 결과**를 붙입니다
  (로그 몇 줄이면 충분합니다)
- 앞선 PR 위에 쌓은 브랜치라면 base를 그 브랜치로 두고, 본문에 순서를 밝힙니다

## 머지

- **Squash merge** 만 사용합니다. `main` 은 PR 하나당 커밋 하나로 유지합니다
- 머지 후 브랜치는 삭제합니다
- 게임 파일을 건드리는 변경(폰트 패치, 설치 경로)은 되돌리는 방법을 PR에 적습니다

## 코드 규칙

### PowerShell (`*.ps1`)

- 인코딩 **UTF-8 BOM**, 줄바꿈 **CRLF** (BOM이 없으면 Windows PowerShell에서 한글이 깨집니다)
- 들여쓰기 4칸, 함수 이름은 `Verb-Noun` (`Get-SteamLibraries`)
- 사용자에게 보이는 문구는 한국어, 색상은 `Cyan`(진행) / `Green`(성공) / `Yellow`(주의) / `Red`(실패) / `DarkGray`(건너뜀)
- 게임 폴더를 찾는 로직을 고쳤다면 `install.ps1`, `font_patch.ps1`, `unlock.ps1`, `build.ps1` 네 곳을 함께 맞춥니다
- 파일을 지우기보다 이름을 바꿔 비활성화합니다 (`winhttp.dll` → `winhttp.dll.disabled`)

### 배치 파일 (`*.bat`)

- 인코딩 **UTF-8 (BOM 없음)**, 첫 줄에 `chcp 65001 >nul`
- 로직은 넣지 않습니다. 같은 이름의 `.ps1` 을 부르기만 합니다

### C# (`src/`)

- 들여쓰기 4칸, 중괄호는 Allman(줄 바꿔서 열기)
- 주석은 **한국어**로, "무엇"이 아니라 "왜"를 적습니다
- 로더에 의존하지 않는 코드는 `Core.cs` 에, 로더별 코드는 `Plugin.<로더>.cs` / `<기능>.<로더>.cs` 로 분리하고 `#if MELON` 으로 가릅니다
- 게임 프레임마다 도는 코드에는 할당을 만들지 않습니다. 매 프레임 들어오는 문자열은 캐시합니다
- 예외로 게임이 죽으면 안 됩니다. 후킹·치환 경로는 `try/catch` 로 감싸고, 반복 실패하면 스스로 꺼집니다

### 빌드 산출물

- `payload/*.dll` 은 손으로 넣지 않습니다. `build.ps1` 로 두 판본을 만들어 복사합니다
- 게임에서 한 번이라도 실행해 참조 어셈블리가 생성돼 있어야 빌드됩니다
  (`BepInEx\interop`, `MelonLoader\Il2CppAssemblies`)

```powershell
.\build.ps1                 # 게임 폴더 자동 탐색
.\build.ps1 -GamePath "D:\SteamLibrary\steamapps\common\Schedule I"
```

### 번역 파일 (`payload/Text/`)

- 인코딩 UTF-8, 형식은 `원문=번역` (XUnity.AutoTranslator 규격)
- `_AutoGeneratedTranslations.txt` 처럼 게임이 만든 파일은 커밋하지 않습니다

## 이슈

- 증상 / 원인 / 해결 방향 / 재현 순서로 적습니다
- 로그를 붙일 때는 관련 구간만 잘라서 코드 블록으로 넣습니다
  (MelonLoader: `MelonLoader\Latest.log`, BepInEx: `BepInEx\LogOutput.log`)
- 게임 버전, 로더 종류와 버전을 적습니다
